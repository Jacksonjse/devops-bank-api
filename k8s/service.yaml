apiVersion: v1
kind: Service
metadata:
  name: bank-api-service
  labels:
    app: bank-api
spec:
  type: NodePort
  selector:
    app: bank-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
